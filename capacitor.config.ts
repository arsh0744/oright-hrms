import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ORight HRMS Cordova',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
